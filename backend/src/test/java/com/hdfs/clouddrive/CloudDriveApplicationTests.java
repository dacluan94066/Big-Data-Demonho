package com.hdfs.clouddrive;

import org.junit.jupiter.api.Test;

/**
 * Basic application context test
 * Note: Test requires MySQL + HDFS running.
 * Skip tests during build: mvn clean install -DskipTests
 */
class CloudDriveApplicationTests {

    @Test
    void contextLoads() {
        // Application context test - requires DB + HDFS
    }
}
